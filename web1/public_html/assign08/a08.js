function toggle(e)
{
   // alert("Calling toggle from" + e);
   // var vis = e.style;

   // if (vis == "none")
   //    vis.display="block";
   // else
   //    vis.display="none";
   // return;
}